project:
    name: kv-movies
    package:
        name:  kv_movies
        api_version: '42.0'
    git: 
        default_branch: master
        prefix_feature: feature/
        prefix_beta: beta/
        prefix_release: release/
    test:
        name_match: _TEST%
    